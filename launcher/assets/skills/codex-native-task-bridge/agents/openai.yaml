interface:
  display_name: "Ponte para tarefas nativas do Codex"
  short_description: "Cria e acompanha tarefas no Codex oficial."
  default_prompt: "Use $codex-native-task-bridge para executar esta validação no chat nativo associado ao contexto atual, reutilizar o mesmo UUID e incluir o link clicável do Codex."
policy:
  allow_implicit_invocation: true

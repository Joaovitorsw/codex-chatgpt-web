interface:
  display_name: "Agendador local do Codex"
  short_description: "Agenda prompts no mesmo chat usando o Windows."
  default_prompt: "Use $codex-local-scheduler para agendar esta instrução no mesmo UUID do Codex usado pelo fluxo atual e incluir o link clicável do chat."
policy:
  allow_implicit_invocation: true
